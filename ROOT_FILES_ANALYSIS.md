# 根目錄文件分析

## 當前根目錄的 .py 和 .sh 文件

### Python 文件 (5個)

#### ✅ 應該保留在根目錄

1. **train.py** (19KB)
   - **用途**: 主訓練入口點（DQN Level 1-5）
   - **為什麼保留**:
     - 這是專案的主要訓練腳本
     - 被 `tools/train_level5_final.sh` 調用
     - 被監控腳本檢查進程
     - 典型的專案根目錄文件
   - **建議**: ✅ 保留

2. **evaluate.py** (16KB)
   - **用途**: 模型評估腳本
   - **為什麼保留**:
     - 與 train.py 配對的評估腳本
     - 被 `tools/analyze_training.sh` 引用
     - 典型的專案根目錄文件
   - **建議**: ✅ 保留

3. **setup_env.sh** (6.8KB)
   - **用途**: 環境設置腳本
   - **為什麼保留**:
     - 初始安裝/配置使用
     - 典型的根目錄腳本
   - **建議**: ✅ 保留

---

#### ⚠️ 可能需要移動或歸檔

4. **train_online_rl.py** (15KB)
   - **用途**: Phase 3 在線 RL 訓練
   - **狀態**:
     - 日期: 2025-10-25（較舊）
     - 可能是舊版本或替代訓練方法
     - 與 train.py 功能重疊
   - **問題**:
     - 名稱暗示這是特定階段的腳本
     - 如果 train.py 已經是統一入口，這個可能過時
   - **建議**:
     - 選項A: 移動到 `scripts/training/`
     - 選項B: 歸檔到 `archive/scripts/training/`
     - 選項C: 如果確認不再使用，可以刪除

5. **train_offline_bc_v4_candidate_pool.py** (17KB)
   - **用途**: Offline Behavior Cloning V4 訓練
   - **狀態**:
     - 日期: 2025-10-25
     - 特定版本的 BC 訓練腳本
     - 名稱很長且特定（v4, candidate_pool）
   - **問題**:
     - 這是 BC（Behavior Cloning）訓練，不是 RL
     - 名稱中的 "v4" 暗示可能有多個版本
     - 根目錄應該放通用腳本，不是特定版本
   - **建議**:
     - 移動到 `scripts/training/bc/` 或
     - 歸檔到 `archive/scripts/bc_training/`

---

## 📊 典型專案結構對比

### 典型的 RL 專案根目錄
```
project/
├── train.py           # ✅ 主訓練腳本
├── evaluate.py        # ✅ 評估腳本
├── setup.py / setup.sh # ✅ 環境設置
├── requirements.txt   # ✅ 依賴
├── README.md          # ✅ 說明
└── src/               # ✅ 源代碼
```

### 我們當前的根目錄
```
handover-rl/
├── train.py                              # ✅ 合理
├── evaluate.py                           # ✅ 合理
├── setup_env.sh                          # ✅ 合理
├── train_online_rl.py                    # ⚠️ 可疑（與train.py重疊？）
├── train_offline_bc_v4_candidate_pool.py # ❌ 不合理（特定版本）
└── ...
```

---

## 🔍 調查問題

需要回答以下問題來決定是否移動：

### train_online_rl.py
1. **與 train.py 的關係**？
   - train.py 是否已經包含在線 RL 訓練？
   - 還是這是不同的訓練方法？

2. **是否還在使用**？
   - 最近的訓練是用哪個腳本？
   - 文檔中推薦使用哪個？

### train_offline_bc_v4_candidate_pool.py
1. **BC 訓練的狀態**？
   - BC 訓練是否已完成？
   - 是否還需要再次運行？

2. **是否有其他版本**？
   - v1, v2, v3 在哪裡？
   - v4 是最終版本嗎？

---

## 💡 建議的清理方案

### 方案 A: 保守方案（推薦）

**保留在根目錄**:
- train.py
- evaluate.py
- setup_env.sh

**創建 scripts/training/ 目錄**:
```
scripts/training/
├── online_rl/
│   └── train_online_rl.py
└── bc/
    └── train_offline_bc_v4_candidate_pool.py
```

**優點**:
- 根目錄只有 3 個核心腳本
- 特定訓練方法組織清晰
- 保留所有功能

---

### 方案 B: 激進方案

**如果確認不再使用**，歸檔到 archive:
```
archive/scripts/training/
├── train_online_rl.py
└── bc/
    └── train_offline_bc_v4_candidate_pool.py
```

**優點**:
- 根目錄最簡潔
- 歷史保留完整

**風險**:
- 如果還需要使用，需要從 archive 恢復

---

### 方案 C: 混合方案

1. **train_online_rl.py**:
   - 如果與 train.py 功能重疊 → archive
   - 如果是獨立的訓練方法 → scripts/training/

2. **train_offline_bc_v4_candidate_pool.py**:
   - 如果 BC 訓練已完成 → archive
   - 如果還在開發中 → scripts/training/bc/

---

## 🎯 推薦行動

我建議你決定：

1. **train_online_rl.py 是否還需要？**
   - 查看: `docs/PROJECT_STATUS.md` 當前使用哪個訓練腳本
   - 確認: 是否已被 train.py 取代

2. **BC 訓練是否已完成？**
   - 查看: `docs/reports/` 下的 BC 訓練報告
   - 確認: 是否還需要重新訓練 BC

根據答案，我可以幫你執行適當的移動操作。
